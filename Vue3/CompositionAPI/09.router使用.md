# router的使用

```vue
<script>
    import { useRouter } from 'vue-router'
    exprot default {
    	setup(){
            const router = useRouter();
            const click = ()=>{
                router.push({name:"routerName"})
            }
            return {
                click
            }
        }
    }
</script>
```

